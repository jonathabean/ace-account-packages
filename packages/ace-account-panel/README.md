# ACE Account Panel

Framework-neutral `<ace-account-panel>` web component used inside each suite
application's native Account & Settings hub.

The host owns authentication, loading, dialog focus, local settings, and
navigation. The component receives only the browser-safe ACE summary and emits
`ace-retry` and `ace-manage` events.
